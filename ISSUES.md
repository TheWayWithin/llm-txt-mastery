# llm-txt-mastery — Issue & Project Register

**This is the single source of truth for what is open in this repo.** One row per
issue/project. Detail lives in the linked doc; this file is the index the Mission
Control reconcile (`repo-reconcile.py`) reads and mirrors to the cockpit.

## ID convention (collision-safe)

Mission Control owns the bare `ISS-`/`PRJ-`/`T-` namespaces. **Every llm-txt-mastery ID
carries the `LTM-` prefix** so it can never collide with a Mission-Control-native
ID or another repo's. Raise issues here with `python3 ~/shared/scripts/repo-issue.py`.

---

## Open

| ID | Title | Status | Severity | Detail | MC-SYNC |
|----|-------|--------|----------|--------|---------|
| LTM-ISS-23 | Unit test makes a live network call and its assertion was weakened to near-nothing: server/services/__tests__/validation.test.ts 'should handle 404 errors gracefully' fetches https://example.com/nonexistent inside a unit test, and commit a8f78c8 replaced the message assertion with expect(result.issues[0].message).toBeTruthy(), which any non-empty string passes. Root cause (network I/O in a unit test) was left in place. Fix: mock fetch and restore a real assertion on the 404 message | ✅ Resolved 2026-08-05 — Fixed in-task: server/services/__tests__/validation.test.ts 404 test now mocks fetch via vi.spyOn instead of hitting example.com, and the assertion is restored from expect(message).toBeTruthy() to expect(message).toContain("doesn't have an llms.txt file yet"). Proven to bite: mutating the expected string turns the test red, restoring it turns it green. 7/7 in file | low | — | pending |
| LTM-ISS-22 | Ten test suites are permanently skipped via describe.skip since Sprint 16 (2026-04-06), including three revenue paths on a live payments product: tests/integration/tier-upgrade-integration.test.ts:47 (12 tests, tier upgrades), client/src/components/__tests__/refund-flow.integration.test.tsx:48 (6 tests, full refund flow), client/src/components/__tests__/InstantRefundModal.test.tsx:29 (6 tests). The suite reports 219 passed / 141 skipped, so the green number overstates real coverage. Marked 'Sprint 16 (issue #23)' as temporary; four months on, unworked. Unskip and repair, or delete with recorded rationale | ✅ Resolved 2026-08-05 — Default suite 219 passed/141 skipped -> 339 passed/0 skipped (main e2b1035). Zero describe.skip/it.skip/test.skip remain anywhere in client, server or tests. Nine of the ten suites revived, one deleted, and no assertion was weakened to buy a pass: several were TIGHTENED from presence checks to exact assembled-textContent equality. Revived: InstantRefundButton 9, InstantRefundModal 12 (incl. a new test for the 3s auto-close and window.location.reload, which nothing asserted, so the downgrade-then-reload step of a refund was untested), refund-flow 6, email-capture 11, email-capture-characterization 28, analyze-characterization 32, home-auth-flow 5, critical-user-flows 16, plus the it.skip in validation-api's tier-expiry test. Root causes were mechanical, not the rewrite the skip notes claimed: (1) endpoint assertions expected a bare '/api/cancel' while the components build ${getApiBaseUrl()}/api/..., which under jsdom resolves to the real production Railway URL; (2) vi.useFakeTimers() was installed in beforeEach and never advanced, so waitFor never ticked and six modal tests were not failing but HANGING to a 30s timeout each; (3) the rest was stale copy after the coffee->solo tier rename. validation-api's tier-expiry test had a genuine logic fault exactly as its note said: one shared 'values' spy meant mock.results[0] compared the first iteration against every later tier. Mutation-proven - setting solo to 99 days fails naming 'solo', not 'starter', which is what proves the per-iteration read is fixed. REAL BUG FOUND AND FIXED (client/src/pages/analyze.tsx): the Solo credit balance was read from two sources on one screen - the Monthly Analyses stat from live usageData, the form's info line from an AuthContext snapshot taken at sign-in - so before usageData loaded a paying customer saw '0 of 20 remaining' beside '5 premium analyses remaining'. Both now read one expression; the characterization test that had pinned the divergence now pins that they agree. DELETED tests/unit/email-captures-validation.test.ts: its spies were never attached (assignments run after the hoisted vi.mock), so all 6 tests fail with 'expected spy to be called' - it has never asserted anything since it was written, and it duplicated tier-upgrade-integration badly. REMAINING GAP, stated plainly: tier-upgrade-integration is no longer skipped but is NOT yet passing. It is excluded from the default config and reachable via a new 'npm run test:db' (vitest.db.config.ts). Run against a real postgres 17 with the schema pushed, 5 of its 6 tests FAIL, so it is deliberately NOT wired into CI - a failing step would turn CI red on every push. The webhook contract 'a tier change must update BOTH emailCaptures and userProfiles' is therefore still enforced nowhere. Full diagnosis is recorded at the top of that file rather than in a new issue row: the solo payload matches neither branch of handleCheckoutCompleted because it omits session.subscription, and the growth/scale cases reach the right branch, return 200, and still leave subscriptionId null, which is unexplained. Also found and NOT fixed, deliberately: tier_selected analytics fires twice per click in email-capture.tsx (both the RadioGroup onValueChange and each card's onClick), double-counting in GA/GTM - every safe fix changes click handling on the highest-conversion signup component; onEmailCaptured and onLoginRequested are required props that are never called; the EMAIL_CAPTURE and AUTH_CHECK states in useFlowStateMachine are unreachable, making a block of home.tsx dead; and retired 'Coffee Power ($4.95/month)' branding still ships beside a radio whose value is 'solo'. Note client/src/test/performance/component-benchmarks.test.tsx asserts wall-clock render time under 100ms and is flaky under parallel load (failed at 114ms while agents ran, 3/3 green idle) | medium | — | pending |
| LTM-ISS-21 | Remote 'develop' branch no longer exists: it was present on 2026-07-30 (reflog confirms checkouts + a merge to main) but 'git ls-remote --heads origin' now returns only main. CLAUDE.md documents develop -> staging auto-deploy (llm-txt-mastery-staging.up.railway.app), so the staging environment currently has no source branch and the documented feature -> develop -> main flow is broken. Decide: recreate develop from main, or update CLAUDE.md/architecture.md to record a main-only flow | ✅ Resolved 2026-08-05 — Fixed in-task: develop recreated from main (git push origin main:refs/heads/develop), so staging has a source branch again and the documented feature->develop->main flow works. develop and main are identical at b1bde70 | medium | — | pending |
| LTM-ISS-20 | Six remote feature branches hold unmerged work: landing-page-phase2, solo-subscription-migration, sprint-10-js-rendering-autodetect, sprint-16-test-suite-recovery, ci-postgres-health-check, sprint10-ci-regression | ✅ Resolved 2026-08-05 — All six branches ARCHIVED, none merged: every one was a duplicate of work already in main. Remote now has exactly one head (main) and 'git branch -r --no-merged origin/main' prints nothing. ROOT CAUSE, and the reason the row existed at all: ancestry is not a valid merged-check in this repo. It squash-merges PRs (a squash makes a new hash, so the source commits are never ancestors of main even when every line landed), AND two patch-identical histories are both reachable from main, diverging at 3791e93 (2025-07-19) - branches were cut from one line while PRs merged from the other. Proof used instead: git patch-id, which hashes diff content not commit identity. All 10 commits across the six branches returned an EXACT patch-id match against a commit already in main, and all 10 matches were independently confirmed as ancestors of origin/main: landing-page-phase2 f9a6cbe->c7f4d11 (2025-10-08); solo-subscription-migration f9f8ab7->75b8b1e and 2625ac6->062dea5 (2026-03-15); sprint-10-js-rendering 515c9be->5565473 and 54a4d67->94594f8 (2026-04-05); sprint-16 all three ->a8f78c8/4dd059d/bc7b21a, real landing was squash-merge 571663d PR #24 (2026-04-06); ci-postgres-health-check 7c2a294->bc3c51b (2026-02-15); sprint10-ci-regression e216154->3fcdf2c (2026-04-06). BILLING: no escalation was needed. solo-subscription-migration is the billing branch and its work is ALREADY LIVE in main (server/routes/auth.ts:227 creditsRemaining 20, COFFEE_TIER_CREDITS=20 at auth.ts:37), verified in main's code independently of patch-id, so archiving it changes no payment behaviour and merging it would have changed none either. Not one line of production code, test, or billing logic was modified by this work. THE 8e4a08c PUZZLE: that merge commit names the sprint-16 branch but neither of its parents is an ancestor of it - it merged a rebased twin, and its tree is byte-identical to its first parent (546b2fd both sides), so it was a literal no-op. Merging any of these branches would have been actively harmful, not merely pointless: landing-page-phase2 would have regressed live pricing copy from the current Solo/Growth/Scale tiers back to the retired COFFEE tier at 4.95/mo, and sprint-16 would have reverted main's newer auth-mock fixes in three test files. RECOVERABILITY: each branch tip was tagged archive/<branch-name> with an annotated reason and pushed BEFORE deletion; all six tags verified present on the remote and pointing at the exact tip (f9a6cbe, f9f8ab7, 515c9be, 9edf87f, 7c2a294, e216154), preserving 1-3 unique commits and 1323-1736 files each. Evidence: tsc exit 0, vitest 219 passed 141 skipped (identical to the pre-work baseline, as expected since no code changed), primary oracle empty. architecture.md gained a section documenting why ancestry lies here, the patch-id recipe, and the archive-tag convention. Follow-ups raised while working: LTM-ISS-21 (remote develop branch has been deleted - staging deploys from it per CLAUDE.md, so the documented feature->develop->main flow is broken), LTM-ISS-22 (10 describe.skip suites live in main since Sprint 16 including three revenue paths - tier upgrades, full refund flow, InstantRefundModal - so 219 green overstates real coverage), LTM-ISS-23 (validation.test.ts makes a live network call and its 404 assertion was weakened to toBeTruthy) | low | — | pending |
| LTM-ISS-19 | Production auto-deploy did not fire on push to main (2026-07-30): LTM-ISS-18 security fix sat undeployed 10+ min until a manual 'railway up' was run, despite the same push-to-deploy working earlier the same day. Staging fired normally. A security or revenue fix can silently sit unshipped. Investigate the Railway GitHub trigger for the production environment and add deploy-failure/no-deploy alerting (relates to LTM-ISS-14's unmet alerting follow-up) | ✅ Resolved 2026-07-30 — Deploy verification + working CI alerting shipped 2026-07-30 (main 8cb27e9, production deploy 5ec82c78 SUCCESS). BIGGER FINDING THAN THE ROW: while proving the new alarm, discovered the repo had NO working alerting at all. .github/workflows/ci-alert.yml omitted the REQUIRED workflows key on its workflow_run trigger, so GitHub rejected the file outright - every run ended in 0s with "this run likely failed because of a workflow file issue" and no alert was ever raised. It had never worked. That explains why LTM-ISS-14 left deploy-failure alerting outstanding and why the LTM-ISS-19 no-deploy went unnoticed: nothing was ever going to turn red. Fixed, and it now watches CI/CD Pipeline, Semantic Features CI/CD Pipeline and Deploy Verify. Note workflow_run always uses the DEFAULT BRANCH copy, so this file must stay valid on main. Deliverable 1, alerting: /health now reports commitSha (RAILWAY_GIT_COMMIT_SHA, "unknown" for manual railway up which has no git context) plus a truthful startedAt - deployedAt previously called new Date() inside the handler, so it echoed request time and could never reveal a stale deploy. New .github/workflows/deploy-verify.yml polls the target /health after every push to main/develop and fails unless the pushed SHA becomes the SHA actually served. Deliberately does NOT read deployment records: the incident created no deployment, so the prior SUCCESS was still latest, and a stale SUCCESS is indistinguishable from a fresh one - asking the service which commit it serves covers a failed deploy AND a deploy that never happened, and needs no Railway API token (no new secret). Includes a workflow_dispatch drill (simulate_failure=true) so the alarm can be re-proven on demand instead of found broken during a real incident. Deliverable 2, diagnosis: NOT root-caused, and I will not invent one. Auto-deploy fired normally on this push (production served 8cb27e9 in 2m47s, deployment timestamped 3 min before verification), so the 2026-07-30 miss appears transient rather than a misconfiguration; the Railway CLI exposes no way to read deploy triggers, so config could not be inspected without dashboard access. The alerting is what makes it survivable either way, which is why it was prioritised. Evidence, all end-to-end not asserted: success path proven on real pushes to BOTH environments (staging 3m6s, production 2m47s, production /health commitSha matching the pushed SHA exactly); failure path proven by drill on both branches (conclusion=failure with the full diagnostic); the alarm chain proven for the first time - drill failure raised GitHub issues #29/#30/#31 "CI failing on main/develop", and a subsequent passing run AUTO-CLOSED them. Also fixed the local failure-path logic before trusting it. architecture.md updated with the mechanism and the default-branch gotcha. Settles the deploy-failure alerting follow-up left unmet by LTM-ISS-14 | medium | — | pending |
| LTM-ISS-18 | Admin credit endpoints (reset/fix-coffee-credits) bypass auth if ADMIN_KEY env unset: 'adminKey !== process.env.ADMIN_KEY' passes when both undefined. Prod has ADMIN_KEY set so not currently exploitable; add explicit 'ADMIN_KEY present' guard as defence-in-depth | ✅ Resolved 2026-07-30 — Fail-open admin auth CLOSED in production 2026-07-30 (main bfb08f1, Railway 822a57ce SUCCESS). SEVERITY CORRECTION: this row said "prod has ADMIN_KEY set so not currently exploitable" - that was wrong. It was true only for the two credit endpoints. The same bug in server/routes/admin-ai-costs.ts was a LIVE unauthenticated exposure: its guard read "adminToken !== process.env.ADMIN_API_TOKEN && NODE_ENV === production", ADMIN_API_TOKEN was set in NEITHER environment, so undefined !== undefined was false, the guard never fired and next() ran; the NODE_ENV clause also disabled it entirely outside production. Measured against production BEFORE the fix: GET /api/admin/ai-costs/summary 200 with JSON, POST /ai-costs/simulation 200, GET /ai-costs/user/:email reached its handler (404) - three admin routes open to anyone, one of them keyed by customer email. Only a status code was taken; no customer data was pulled. Fix: new server/middleware/admin-secret-auth.ts, a shared guard that fails CLOSED (missing or blank secret answers 403, never passes), enforced in EVERY NODE_ENV, compared with crypto.timingSafeEqual behind a length check, returning an identical 403 body whether the server is misconfigured or the caller is wrong so it cannot be used as an oracle. Applied to all three ai-costs routes and both /api/auth/admin/*-coffee-credits handlers. ADMIN_API_TOKEN generated and set in both Railway environments (64 chars, never printed). Judgement calls: (1) startup validation reports missing admin secrets as errors NOT criticalErrors - the runtime guard already makes a missing secret safe, so refusing to boot would trade contained degradation for a total outage of a live product; (2) kept the write path and response shapes identical so the 10 existing LTM-ISS-3 tests still pin real behaviour; (3) the pre-commit credential scanner flagged a test fixture - fixed the fixture rather than weakening the scanner; (4) tracked .railwayignore, previously left untracked, after it went missing and blocked the fallback deploy. Evidence: 12 regression-first tests (204 suite-wide, tsc 0), 5 of them confirmed FAILING against the old guard before the fix existed; staging deploy 8af7f43f then production 822a57ce; production probe went 2/5 -> 5/5 with G/H/I flipping 200/404 -> 403; and the counter-check that the fix did not brick the endpoints - correct token 200 with JSON, one-character tamper 403, no token 403, verified on both staging and production. Probe extended (scripts/probe-reset-credits.mjs checks G/H/I) and made production-safe by hard-disabling only the mutating check there, so it now doubles as a production regression check. Raised LTM-ISS-19: the push to main triggered NO production deploy for 10+ minutes while staging fired normally, so this fix had to be shipped with a manual railway up - a security fix can silently sit unshipped; architecture.md updated to say verify the deployment TIMESTAMP, not just its status | low | — | pending |
| LTM-ISS-17 | Deliberate stripe-node SDK upgrade 18.5.0 -> 22.x (pins 2026-06-24.dahlia): audit every direct API call site, and make cancellation.ts treat cancel_at as a cancellation signal alongside cancel_at_period_end (flexible billing_mode becomes creation-default past clover — see docs/Operations/stripe-api-version-audit-2026-07.md watch-item 1) | ✅ Resolved 2026-07-29 — stripe-node 18.5.0 -> 22.3.2 shipped to production 2026-07-29 (main b27c5af, Railway ac2c1837 SUCCESS). SDK now pins 2026-06-24.dahlia, matching the production webhook endpoint, so direct API reads and webhook payloads share an API version for the first time. Call-site audit: all 21 direct call sites (services/stripe.ts, services/cancellation.ts, routes/stripe.ts) checked against the shipped .d.ts of 22.3.2 - every one unaffected, so the only required change was the typed PINNED_STRIPE_API_VERSION literal. v19/v20 breaks are V2-API/thin-event only; v21 Stripe.Decimal retyping does not touch us (we read unit_amount, an integer - no *_decimal field is read anywhere); v22 removed callbacks, positional API keys, per-request host and the types/ dir, none of which we used. Verified the clean tsc is REAL not a false green: moduleResolution=bundler resolves v22 exports-map types (types/ dir and package.json types field are both gone in v22). Behaviour fix, the actual risk in this jump: flexible billing_mode is the creation default from 2025-09-30.clover, so every subscription created under the dahlia pin is flexible, and Stripe sets cancel_at while leaving cancel_at_period_end FALSE for a Customer Portal cancellation - the old cancel_at_period_end-only check at cancellation.ts:454 missed real cancellations. Added isCancellationScheduled() and effectiveSubscriptionEndAt() (cancel_at wins; flexible resolves it once and never re-derives it), read the end date off the update response instead of the stale pre-update object, and fixed subscriptionPeriodEnd() to take the LATEST item period end. Write path still sends cancel_at_period_end:true - deprecated but accepted and behaviourally unchanged, deliberately left alone. Judgement call: pinned stripe EXACTLY (was ^22.3.2) because 22.4.0 pins 2026-07-29.dahlia and the single-literal apiVersion type means a caret bump would both fail tsc and re-misalign the SDK from the endpoint. Evidence: staging deploys a59d81a8 + bbef93a8 SUCCESS; test-mode subscription.updated and invoice.payment_succeeded processed by the deployed dahlia build (logs "Subscription updated for user: 3, tier: solo, status: active" and "Payment recorded"), writing subscriptions row 4 and payment_history row 34; scripts/verify-stripe-dahlia-cancellation.ts 9/9 importing the REAL predicate against live test-mode Stripe (flexible is the default, portal-style cancel_at=max_period_end sets cancel_at with cancel_at_period_end false, old check misses it, new one detects it, both write paths still work); 13 new unit tests (23 in file, 192 suite-wide) proven to fail when the old predicate is reinstated; tsc 0; production probe 5/5 confirming the upgraded webhooks.constructEvent signature path. Audit doc updated: docs/Operations/stripe-api-version-audit-2026-07.md watch-item 1 resolved plus a full SDK-upgrade section | low | — | pending |
| LTM-ISS-16 | Stripe webhook endpoints pinned to 2020-08-27: audit 2026-06-24.dahlia before migrating. Dashboard no longer offers 2025-08-27.basil (only current 2020-08-27, dahlia latest, dahlia preview), so the new production live endpoint was created at 2020-08-27 — the shape the dual-shape code was staging-verified against. dahlia is ~10 months newer than the basil audit in LTM-ISS-8 and may change consumed fields (invoice.parent, subscription items period, invoice.payments). Audit the changelog 2025-08-27.basil -> 2026-06-24.dahlia, extend tests/unit/stripe-basil-shapes.test.ts, verify on staging, then flip prod endpoint version | ✅ Resolved 2026-07-28 — Production Stripe webhooks now on 2026-06-24.dahlia. Sourced field audit (docs/Operations/stripe-api-version-audit-2026-07.md): 10 releases after basil, zero consumed-field changes. Staging proven at dahlia first (endpoint we_1TyEjT..., events 200, linked payment_history row 33). Dashboard no longer updates endpoint versions in place, so migrated per Stripe's parallel-endpoint pattern: new live endpoint we_1TyFqQIiC84gpR8HpTDnAGpb (dahlia, 5 events) + secret rotated into Railway (deploy d4e772b8). Probes 5/5: health 200, unsigned POST 400 missing-signature, bogus signature 400 via HMAC. Old 2020-08-27 endpoint to be disabled by Jamie; first real delivery check in Stripe dashboard. SDK upgrade deferred as LTM-ISS-17 | medium | — | pending |
| LTM-ISS-15 | Production has NO live Stripe webhook endpoint: live mode lists only evolve-7/supabase/waywithin destinations, none for llm-txt-mastery-production — subscription/invoice webhooks have never been delivered in production (prod STRIPE_WEBHOOK_SECRET is orphaned). Explains missing prod payment_history linkage. Fix rides with LTM-ISS-8 rollout: create live endpoint at 2025-08-27.basil + set prod secret + deploy | ✅ Resolved 2026-07-27 — Live webhook endpoint created 2026-07-28 in Aisearchmastery live account: we_1Ty164IiC84gpR8HkSSfUgXQ, enabled, livemode, url https://llm-txt-mastery-production.up.railway.app/api/stripe/webhook, api_version 2020-08-27, exactly the five required events (checkout.session.completed, customer.subscription.updated, customer.subscription.deleted, invoice.payment_succeeded, invoice.payment_failed), no extras. Prod STRIPE_WEBHOOK_SECRET replaced with that endpoint's signing secret in Railway production (whsec_ prefix verified without printing the value); Railway redeployed 0228eb00 SUCCESS 2026-07-28T02:54Z. Verified by scripts/probe-stripe-webhook.mjs: 5/5 - health 200, unsigned POST 400 missing-signature, bogus-signature 400 via the signature-validation branch (proves a secret is loaded and HMAC runs, not the STRIPE_WEBHOOK_SECRET-is-required branch) | high | — | pending |
| LTM-ISS-14 | Railway backend deploys silently FAILING since ~23 Jul: npm ci (prod, no devDeps) runs the husky prepare script, husky binary absent, exit 127 — every backend deploy since the husky v9 bootstrap fix died at build; production ran a frozen pre-23-Jul build while Netlify kept deploying the frontend. Fix: HUSKY=0 service variable on Railway (husky v9 no-ops). Add deploy-failure alerting so this cannot go unnoticed for days again | ✅ Resolved 2026-07-26 — HUSKY prepare guarded via .husky/install.mjs (husky v9 production pattern) + hook hardened for sh -e runners; deploy 86ee605f SUCCESS 2026-07-26 14:42 — first green backend deploy since ~23 Jul, backend now current with main. Deploy-failure alerting still wanted (follow-up in row text) | high | — | pending |
| LTM-ISS-13 | Production observed LCP still ~6.8s / LH .57 (devtools throttling) despite warm-local 0.97/2.1s: real-network render path pays for render-blocking fonts.googleapis CSS (two extra TLS origins) and the 87KB main stylesheet. Needs a font strategy that does not regress Lighthouse's simulated model (self-hosting prototype on branch history showed lantern mis-scores it 9.7s) plus CSS size work | ✅ Resolved 2026-07-28 — Shipped 1e69802, live on llmtxtmastery.com 2026-07-28. Production re-measure (scripts/measure-lcp.mjs, medians of 3): devtools score 0.57->0.90, LCP ~6.8s->2.0s; simulated 0.99 / LCP 1.6s. Root causes: prerender-serialised blocking js.stripe.com tag stripped, fonts self-hosted (single 48KB Inter variable, google origins gone), hydrateRoot reuses prerendered DOM (createRoot wipe was CLS 0.75; fixed 49 nested-anchor Links + text-node + style mismatches), consent-banner text capped so hero stays LCP element | medium | — | pending |
| LTM-ISS-12 | Canonical tag mismatch: head declares https://www.llmtxtmastery.com while the site serves non-www — scanner flags cross-domain canonical (40/100) and www/non-www inconsistency; fix canonical (and per-page canonicals) as part of the LTM-ISS-10 meta work | ✅ Resolved 2026-07-26 — Shipped (c79b048, deployed 2026-07-26): useSEO stamps self-referential non-www canonical + og:url per route from shared SITE_URL; all 9 live routes verified; www 301s with path preserved. Scanner: canonical 40->100, indexability 85->100, warnings cleared; overall 81->82 | low | — | pending |
| LTM-ISS-11 | Perf + policy gaps on llmtxtmastery.com: LCP 6.5s (LH perf .70), robots.txt has no explicit AI-crawler policy (GPTBot/ClaudeBot/PerplexityBot/Google-Extended unnamed) | ✅ Resolved 2026-07-26 — Shipped (ec116c2, deployed 2026-07-26): robots.txt names GPTBot/OAI-SearchBot/ClaudeBot/Claude-Web/PerplexityBot/Google-Extended with explicit Allow (scanner robots factor 100). Perf target (warm local prod build) met by the ISS-9 prerender: LH 0.97/LCP 2.1s observed; font self-hosting prototyped, measured no-win, deliberately dropped (lantern artefact documented in commit). Production real-network render path tracked as LTM-ISS-13 | medium | — | pending |
| LTM-ISS-10 | No structured data on llmtxtmastery.com: zero ld+json (add Organization/WebSite/SoftwareApplication + FAQPage if honest), no date metadata, llms.txt undated | ✅ Resolved 2026-07-25 — Shipped (debbd07, deployed 2026-07-25): Organization+WebSite JSON-LD site-wide, SoftwareApplication with real tier prices on / and /pricing, FAQPage from the same constant as the visible FAQ; Content-updated dates with time[datetime] on all marketing pages; llms.txt + llms-full.txt dated. AImpactScanner rescan: 73 -> 81 Excellent (structured data 30->100, FAQ 60->100, freshness 30->100, transparency 35->70). Register updated; remaining: LTM-ISS-11/12 | medium | — | pending |
| LTM-ISS-9 | llmtxtmastery.com is a client-rendered SPA: non-JS crawlers (and AImpactScanner: 36/100 F, heading hierarchy 0, content depth 5) see an empty shell — the llms.txt product is itself near-invisible to AI. Add prerendering/SSG for the marketing pages | ✅ Resolved 2026-07-25 — Prerender shipped (commit 7cf915a, deployed 2026-07-25): 9 marketing routes serve full HTML to non-JS crawlers (/ = 82.5KB with h1, /pricing 44.9KB, etc.; tool routes stay SPA shell). AImpactScanner rescan: 36/100 F -> 73/100 Good; headings 0->80, content depth 5->95, citation structure 100. Register updated; follow-ups LTM-ISS-10/11/12 | high | — | pending |
| LTM-ISS-8 | Stripe SDK v18+ types pin apiVersion 2025-08-27.basil but the client pins 2024-06-20 (kept via documented cast to avoid changing live payment behaviour). Plan a deliberate API-version migration: review breaking changes, update webhook expectations, test in staging | ✅ Resolved 2026-07-27 — Stripe API-version migration landed in production 2026-07-28. main a1ef39e carries the dual-shape code (SDK pinned 2025-08-27.basil, cast removed, 10 pinning tests); staging verified both old (2020-08-27) and basil payload shapes end-to-end with real DB writes. Production rollout completed with LTM-ISS-15: live endpoint we_1Ty164IiC84gpR8HkSSfUgXQ created and prod secret set, deployment 0228eb00 SUCCESS, probe 5/5 green. Judgement call: the Stripe dashboard no longer offers 2025-08-27.basil (only 2020-08-27 current, 2026-06-24.dahlia latest, dahlia preview), so the endpoint was created at 2020-08-27 - the shape the deployed dual-shape code was staging-verified against - rather than the unaudited dahlia. dahlia audit tracked as LTM-ISS-16 | medium | — | pending |
| LTM-ISS-7 | Payment history rows from the subscription-updated webhook never stored the Stripe subscription id or tier (keys not in the insert schema; drizzle dropped them silently). Decide the linkage (map Stripe sub id -> local subscriptions.id FK) and add it deliberately with a test | ✅ Resolved 2026-07-26 — Payment linkage live: merged 34b01c6, deployed in 86ee605f (after unblocking LTM-ISS-14). handleSubscriptionUpdate upserts/links the local subscriptions row into paymentHistory.subscriptionId, null-fallback never blocks the webhook; pinned by 4 unit tests against the real handler. Runtime spot-check on next real subscription event: SELECT id, subscription_id, amount, created_at FROM payment_history ORDER BY id DESC LIMIT 5 | medium | — | pending |
| LTM-ISS-6 | getUserUsageStats query references table email_captures but the real table name is emailCaptures — the stats query has always failed and returned null; fix the query (and add a test) if the stats are wanted | ✅ Resolved 2026-07-29 — Bigger than recorded: the same camelCase-quoting bug was live in three /api/admin/ai-costs/* queries (admin-ai-costs.ts:39,67,188), which had been throwing and returning 500 since they were written - the admin cost dashboard had never worked. Fixed those to JOIN "emailCaptures". Deleted getUserUsageStats rather than repairing it: zero callers repo-wide, no route exposes it, and it was wrong on TWO counts - unquoted email_captures AND matching usage_tracking.user_id (a FK to users.id, written as emailCaptures.user_id per routes.ts:182) against SELECT id FROM emailCaptures, so fixing only the quoting would have produced confidently wrong numbers. Rationale left in situ at the deletion site; architecture.md schema-quirks note updated. Also quoted 4 dead refs in scripts/setup-pgvector.sql. Proven against the STAGING Neon db: unquoted -> 'relation email_captures does not exist', quoted -> 51 rows, and the fixed admin join returns real data. Guard: tests/unit/raw-sql-table-quoting.test.ts derives the camelCase table list from shared/schema.ts and fails on any unquoted raw-SQL reference in server/ or shared/ (comment-stripped to avoid false positives), proven to catch a reintroduced JOIN email_captures. tsc 0, vitest 179 passed. Branch feature/ltm-iss6-usage-stats-query | low | — | pending |
| LTM-ISS-5 | API v1 processAnalysis pipeline is broken against the current sitemap/analyzer APIs: reads sitemapResult.pages/.found/.source (now entries/sitemapFound/analysisMethod), passes userTier where analyzeDiscoveredPagesWithCache expects userEmail, and treats its {pages,metrics} return as an array — every /api/v1 analysis crashes at the first metadata update. Needs a deliberate repair + test (which email/identity to use for usage tracking) | ✅ Resolved 2026-07-25 — processAnalysis repaired against current SitemapResult/analyzer contracts in 765b178 with 4 pinning tests (completed flow, identity fallback, JS-render quota, failure path); identity = API consumer label, analyzer email lookups fail open as for any unknown identity | medium | — | pending |
| LTM-ISS-4 | verify-email: 'already logged in' fast-path after verification has never executed (refreshUser() resolves void, and it reads pendingAnalysisUrl after removing it). Decide whether to revive it by checking auth context user | ✅ Resolved 2026-07-28 — Revived the fast-path rather than deleting it, on evidence: registration stores tokens in sessionStorage (authApi.register -> setTokens), so the tab that performed the signup still holds a live session, and login.tsx already accepts ?websiteUrl= and forwards it to /analyze after sign-in. verify-email.tsx now reads pendingAnalysisUrl BEFORE clearing the keys and routes via a pure resolveVerifiedRedirect(): authenticated -> /analyze?url=..., otherwise -> /login?verified=true&websiteUrl=..., so intent survives both paths and the key is no longer orphaned. 'Authenticated' is defined in the code as authApi.isAuthenticated() (live access token AND stored user in this tab's sessionStorage), read at redirect time rather than from the mount-effect closure - that stale/void read is what made the branch dead. Cover: client/src/pages/__tests__/verify-email-redirect.test.tsx, 8 tests pinning both branches, proven to fail when the old always-/login behaviour is reinstated; plus tests/verify-email-redirect-flow.mjs driving both paths in a real browser (2/2). tsc 0, vitest 174 passed. Branch feature/ltm-iss4-verify-email-fastpath | low | — | pending |
| LTM-ISS-3 | Verify /api/auth/admin/reset-credits endpoint end-to-end (was crashing on undefined COFFEE_TIER_CREDITS; constant now defined at module scope = 20) | ✅ Resolved 2026-07-28 — Admin credit-reset endpoint verified end-to-end. Path correction: real route is /api/auth/admin/reset-coffee-credits (the row name /reset-credits does not exist). Read reset handler + sibling /admin/fix-coffee-credits + handleSubscriptionRenewal; COFFEE_TIER_CREDITS=20=TIER_LIMITS.solo.dailyAnalyses. Live on staging via scripts/probe-reset-credits.mjs, 6 of 6 branches green: no-key 403, wrong-key 403, no-email 400 EMAIL_REQUIRED, unknown-user 404 USER_NOT_FOUND, non-solo 400 NOT_SOLO_TIER (userTier echoed), success 200 (previousCredits 19 -> newCredits 20; the endpoint read and wrote the real staging DB row for jamie.watters.mail@icloud.com, confirming the live DB state change). Branch 6 also proven by tests/unit/admin-reset-credits.test.ts against identical deployed code: sets creditsRemaining=20 + calls handleSubscriptionRenewal(user.id), accepts legacy coffee alias, returns 500 (not a silent crash) on storage error. The probe runs the mutating success check only as an opt-in (RESET_TARGET_EMAIL) and refuses any production URL. Artifacts left: probe (6 branches) + 10 unit tests; full suite 174 passed 0 failed. No code defect (original ReferenceError already fixed by the module-scope constant). Hardening spotted and raised as LTM-ISS-18 (low): if ADMIN_KEY env is unset the guard passes a no-key request; prod has ADMIN_KEY set so not exploitable now | low | — | pending |
| LTM-ISS-2 | Burn down the 261-error TS baseline (.tsc-baseline.txt) to zero and restore full tsc gate | ✅ Resolved 2026-07-25 — 261 -> 0 across 12 root-cause batches (4344073..765b178): types made true, no any/ts-ignore/tsconfig loosening; baseline + shim removed, full 'npx tsc --noEmit' husky gate restored and proven (clean commit passes, scratch error rejected, gitleaks blocks a canary secret). Payment paths minimally changed in flagged commits fa2f86d/7ca34df. New rows raised: LTM-ISS-3/4/6/7/8 | low | — | pending |
| LTM-ISS-1 | Pre-commit hook now catches ~40 pre-existing TypeScript errors blocking normal commits | ✅ Resolved 2026-07-23 — Commits unblocked: husky TS check now gates against committed error baseline (.tsc-baseline.txt, 261 pre-existing errors); new TS errors and secrets still blocked (all three hook tests proven), commit 2ff2639 | medium | — | pending |

## Recently closed

| ID | Title | Status | Commit | Detail |
|----|-------|--------|--------|--------|
