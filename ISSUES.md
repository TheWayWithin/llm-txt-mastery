# llm-txt-mastery — Issue & Project Register

**This is the single source of truth for what is open in this repo.** One row per
issue/project. Detail lives in the linked doc; this file is the index the Mission
Control reconcile (`repo-reconcile.py`) reads and mirrors to the cockpit.

## ID convention (collision-safe)

Mission Control owns the bare `ISS-`/`PRJ-`/`T-` namespaces. **Every llm-txt-mastery ID
carries the `LTM-` prefix** so it can never collide with a Mission-Control-native
ID or another repo's. Raise issues here with `python3 ~/shared/scripts/repo-issue.py`.

---

## Open

| ID | Title | Status | Severity | Detail | MC-SYNC |
|----|-------|--------|----------|--------|---------|
| LTM-ISS-17 | Deliberate stripe-node SDK upgrade 18.5.0 -> 22.x (pins 2026-06-24.dahlia): audit every direct API call site, and make cancellation.ts treat cancel_at as a cancellation signal alongside cancel_at_period_end (flexible billing_mode becomes creation-default past clover — see docs/Operations/stripe-api-version-audit-2026-07.md watch-item 1) | Open | low | — | pending |
| LTM-ISS-16 | Stripe webhook endpoints pinned to 2020-08-27: audit 2026-06-24.dahlia before migrating. Dashboard no longer offers 2025-08-27.basil (only current 2020-08-27, dahlia latest, dahlia preview), so the new production live endpoint was created at 2020-08-27 — the shape the dual-shape code was staging-verified against. dahlia is ~10 months newer than the basil audit in LTM-ISS-8 and may change consumed fields (invoice.parent, subscription items period, invoice.payments). Audit the changelog 2025-08-27.basil -> 2026-06-24.dahlia, extend tests/unit/stripe-basil-shapes.test.ts, verify on staging, then flip prod endpoint version | ✅ Resolved 2026-07-28 — Production Stripe webhooks now on 2026-06-24.dahlia. Sourced field audit (docs/Operations/stripe-api-version-audit-2026-07.md): 10 releases after basil, zero consumed-field changes. Staging proven at dahlia first (endpoint we_1TyEjT..., events 200, linked payment_history row 33). Dashboard no longer updates endpoint versions in place, so migrated per Stripe's parallel-endpoint pattern: new live endpoint we_1TyFqQIiC84gpR8HpTDnAGpb (dahlia, 5 events) + secret rotated into Railway (deploy d4e772b8). Probes 5/5: health 200, unsigned POST 400 missing-signature, bogus signature 400 via HMAC. Old 2020-08-27 endpoint to be disabled by Jamie; first real delivery check in Stripe dashboard. SDK upgrade deferred as LTM-ISS-17 | medium | — | pending |
| LTM-ISS-15 | Production has NO live Stripe webhook endpoint: live mode lists only evolve-7/supabase/waywithin destinations, none for llm-txt-mastery-production — subscription/invoice webhooks have never been delivered in production (prod STRIPE_WEBHOOK_SECRET is orphaned). Explains missing prod payment_history linkage. Fix rides with LTM-ISS-8 rollout: create live endpoint at 2025-08-27.basil + set prod secret + deploy | ✅ Resolved 2026-07-27 — Live webhook endpoint created 2026-07-28 in Aisearchmastery live account: we_1Ty164IiC84gpR8HkSSfUgXQ, enabled, livemode, url https://llm-txt-mastery-production.up.railway.app/api/stripe/webhook, api_version 2020-08-27, exactly the five required events (checkout.session.completed, customer.subscription.updated, customer.subscription.deleted, invoice.payment_succeeded, invoice.payment_failed), no extras. Prod STRIPE_WEBHOOK_SECRET replaced with that endpoint's signing secret in Railway production (whsec_ prefix verified without printing the value); Railway redeployed 0228eb00 SUCCESS 2026-07-28T02:54Z. Verified by scripts/probe-stripe-webhook.mjs: 5/5 - health 200, unsigned POST 400 missing-signature, bogus-signature 400 via the signature-validation branch (proves a secret is loaded and HMAC runs, not the STRIPE_WEBHOOK_SECRET-is-required branch) | high | — | pending |
| LTM-ISS-14 | Railway backend deploys silently FAILING since ~23 Jul: npm ci (prod, no devDeps) runs the husky prepare script, husky binary absent, exit 127 — every backend deploy since the husky v9 bootstrap fix died at build; production ran a frozen pre-23-Jul build while Netlify kept deploying the frontend. Fix: HUSKY=0 service variable on Railway (husky v9 no-ops). Add deploy-failure alerting so this cannot go unnoticed for days again | ✅ Resolved 2026-07-26 — HUSKY prepare guarded via .husky/install.mjs (husky v9 production pattern) + hook hardened for sh -e runners; deploy 86ee605f SUCCESS 2026-07-26 14:42 — first green backend deploy since ~23 Jul, backend now current with main. Deploy-failure alerting still wanted (follow-up in row text) | high | — | pending |
| LTM-ISS-13 | Production observed LCP still ~6.8s / LH .57 (devtools throttling) despite warm-local 0.97/2.1s: real-network render path pays for render-blocking fonts.googleapis CSS (two extra TLS origins) and the 87KB main stylesheet. Needs a font strategy that does not regress Lighthouse's simulated model (self-hosting prototype on branch history showed lantern mis-scores it 9.7s) plus CSS size work | ✅ Resolved 2026-07-28 — Shipped 1e69802, live on llmtxtmastery.com 2026-07-28. Production re-measure (scripts/measure-lcp.mjs, medians of 3): devtools score 0.57->0.90, LCP ~6.8s->2.0s; simulated 0.99 / LCP 1.6s. Root causes: prerender-serialised blocking js.stripe.com tag stripped, fonts self-hosted (single 48KB Inter variable, google origins gone), hydrateRoot reuses prerendered DOM (createRoot wipe was CLS 0.75; fixed 49 nested-anchor Links + text-node + style mismatches), consent-banner text capped so hero stays LCP element | medium | — | pending |
| LTM-ISS-12 | Canonical tag mismatch: head declares https://www.llmtxtmastery.com while the site serves non-www — scanner flags cross-domain canonical (40/100) and www/non-www inconsistency; fix canonical (and per-page canonicals) as part of the LTM-ISS-10 meta work | ✅ Resolved 2026-07-26 — Shipped (c79b048, deployed 2026-07-26): useSEO stamps self-referential non-www canonical + og:url per route from shared SITE_URL; all 9 live routes verified; www 301s with path preserved. Scanner: canonical 40->100, indexability 85->100, warnings cleared; overall 81->82 | low | — | pending |
| LTM-ISS-11 | Perf + policy gaps on llmtxtmastery.com: LCP 6.5s (LH perf .70), robots.txt has no explicit AI-crawler policy (GPTBot/ClaudeBot/PerplexityBot/Google-Extended unnamed) | ✅ Resolved 2026-07-26 — Shipped (ec116c2, deployed 2026-07-26): robots.txt names GPTBot/OAI-SearchBot/ClaudeBot/Claude-Web/PerplexityBot/Google-Extended with explicit Allow (scanner robots factor 100). Perf target (warm local prod build) met by the ISS-9 prerender: LH 0.97/LCP 2.1s observed; font self-hosting prototyped, measured no-win, deliberately dropped (lantern artefact documented in commit). Production real-network render path tracked as LTM-ISS-13 | medium | — | pending |
| LTM-ISS-10 | No structured data on llmtxtmastery.com: zero ld+json (add Organization/WebSite/SoftwareApplication + FAQPage if honest), no date metadata, llms.txt undated | ✅ Resolved 2026-07-25 — Shipped (debbd07, deployed 2026-07-25): Organization+WebSite JSON-LD site-wide, SoftwareApplication with real tier prices on / and /pricing, FAQPage from the same constant as the visible FAQ; Content-updated dates with time[datetime] on all marketing pages; llms.txt + llms-full.txt dated. AImpactScanner rescan: 73 -> 81 Excellent (structured data 30->100, FAQ 60->100, freshness 30->100, transparency 35->70). Register updated; remaining: LTM-ISS-11/12 | medium | — | pending |
| LTM-ISS-9 | llmtxtmastery.com is a client-rendered SPA: non-JS crawlers (and AImpactScanner: 36/100 F, heading hierarchy 0, content depth 5) see an empty shell — the llms.txt product is itself near-invisible to AI. Add prerendering/SSG for the marketing pages | ✅ Resolved 2026-07-25 — Prerender shipped (commit 7cf915a, deployed 2026-07-25): 9 marketing routes serve full HTML to non-JS crawlers (/ = 82.5KB with h1, /pricing 44.9KB, etc.; tool routes stay SPA shell). AImpactScanner rescan: 36/100 F -> 73/100 Good; headings 0->80, content depth 5->95, citation structure 100. Register updated; follow-ups LTM-ISS-10/11/12 | high | — | pending |
| LTM-ISS-8 | Stripe SDK v18+ types pin apiVersion 2025-08-27.basil but the client pins 2024-06-20 (kept via documented cast to avoid changing live payment behaviour). Plan a deliberate API-version migration: review breaking changes, update webhook expectations, test in staging | ✅ Resolved 2026-07-27 — Stripe API-version migration landed in production 2026-07-28. main a1ef39e carries the dual-shape code (SDK pinned 2025-08-27.basil, cast removed, 10 pinning tests); staging verified both old (2020-08-27) and basil payload shapes end-to-end with real DB writes. Production rollout completed with LTM-ISS-15: live endpoint we_1Ty164IiC84gpR8HkSSfUgXQ created and prod secret set, deployment 0228eb00 SUCCESS, probe 5/5 green. Judgement call: the Stripe dashboard no longer offers 2025-08-27.basil (only 2020-08-27 current, 2026-06-24.dahlia latest, dahlia preview), so the endpoint was created at 2020-08-27 - the shape the deployed dual-shape code was staging-verified against - rather than the unaudited dahlia. dahlia audit tracked as LTM-ISS-16 | medium | — | pending |
| LTM-ISS-7 | Payment history rows from the subscription-updated webhook never stored the Stripe subscription id or tier (keys not in the insert schema; drizzle dropped them silently). Decide the linkage (map Stripe sub id -> local subscriptions.id FK) and add it deliberately with a test | ✅ Resolved 2026-07-26 — Payment linkage live: merged 34b01c6, deployed in 86ee605f (after unblocking LTM-ISS-14). handleSubscriptionUpdate upserts/links the local subscriptions row into paymentHistory.subscriptionId, null-fallback never blocks the webhook; pinned by 4 unit tests against the real handler. Runtime spot-check on next real subscription event: SELECT id, subscription_id, amount, created_at FROM payment_history ORDER BY id DESC LIMIT 5 | medium | — | pending |
| LTM-ISS-6 | getUserUsageStats query references table email_captures but the real table name is emailCaptures — the stats query has always failed and returned null; fix the query (and add a test) if the stats are wanted | Open | low | — | pending |
| LTM-ISS-5 | API v1 processAnalysis pipeline is broken against the current sitemap/analyzer APIs: reads sitemapResult.pages/.found/.source (now entries/sitemapFound/analysisMethod), passes userTier where analyzeDiscoveredPagesWithCache expects userEmail, and treats its {pages,metrics} return as an array — every /api/v1 analysis crashes at the first metadata update. Needs a deliberate repair + test (which email/identity to use for usage tracking) | ✅ Resolved 2026-07-25 — processAnalysis repaired against current SitemapResult/analyzer contracts in 765b178 with 4 pinning tests (completed flow, identity fallback, JS-render quota, failure path); identity = API consumer label, analyzer email lookups fail open as for any unknown identity | medium | — | pending |
| LTM-ISS-4 | verify-email: 'already logged in' fast-path after verification has never executed (refreshUser() resolves void, and it reads pendingAnalysisUrl after removing it). Decide whether to revive it by checking auth context user | ✅ Resolved 2026-07-28 — Revived the fast-path rather than deleting it, on evidence: registration stores tokens in sessionStorage (authApi.register -> setTokens), so the tab that performed the signup still holds a live session, and login.tsx already accepts ?websiteUrl= and forwards it to /analyze after sign-in. verify-email.tsx now reads pendingAnalysisUrl BEFORE clearing the keys and routes via a pure resolveVerifiedRedirect(): authenticated -> /analyze?url=..., otherwise -> /login?verified=true&websiteUrl=..., so intent survives both paths and the key is no longer orphaned. 'Authenticated' is defined in the code as authApi.isAuthenticated() (live access token AND stored user in this tab's sessionStorage), read at redirect time rather than from the mount-effect closure - that stale/void read is what made the branch dead. Cover: client/src/pages/__tests__/verify-email-redirect.test.tsx, 8 tests pinning both branches, proven to fail when the old always-/login behaviour is reinstated; plus tests/verify-email-redirect-flow.mjs driving both paths in a real browser (2/2). tsc 0, vitest 174 passed. Branch feature/ltm-iss4-verify-email-fastpath | low | — | pending |
| LTM-ISS-3 | Verify /api/auth/admin/reset-credits endpoint end-to-end (was crashing on undefined COFFEE_TIER_CREDITS; constant now defined at module scope = 20) | Open | low | — | pending |
| LTM-ISS-2 | Burn down the 261-error TS baseline (.tsc-baseline.txt) to zero and restore full tsc gate | ✅ Resolved 2026-07-25 — 261 -> 0 across 12 root-cause batches (4344073..765b178): types made true, no any/ts-ignore/tsconfig loosening; baseline + shim removed, full 'npx tsc --noEmit' husky gate restored and proven (clean commit passes, scratch error rejected, gitleaks blocks a canary secret). Payment paths minimally changed in flagged commits fa2f86d/7ca34df. New rows raised: LTM-ISS-3/4/6/7/8 | low | — | pending |
| LTM-ISS-1 | Pre-commit hook now catches ~40 pre-existing TypeScript errors blocking normal commits | ✅ Resolved 2026-07-23 — Commits unblocked: husky TS check now gates against committed error baseline (.tsc-baseline.txt, 261 pre-existing errors); new TS errors and secrets still blocked (all three hook tests proven), commit 2ff2639 | medium | — | pending |

## Recently closed

| ID | Title | Status | Commit | Detail |
|----|-------|--------|--------|--------|
